# Native cross-build output
native/out/
native/toolchains/

# Firmware build matrix output
firmware/out/

# Gradle / Android
.gradle/
build/
local.properties
*.apk
*.aab
*.keystore
!debug.keystore

# Release signing material. The keystore IS the app's identity -- if this is
# ever committed, the key is burned and every future update is compromised.
secrets/
.idea/
captures/

# Staged build products (regenerate with native/build-pm3.sh)
app/src/main/jniLibs/
app/src/main/assets/pm3-resources.zip
